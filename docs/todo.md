1. [x] 画像生成のトリガーがGAMEOVERタイミングだと遅いので、カメラ撮影が確定した瞬間にphoto_yyyymmdd_hhmmss.pngが生成された時に実行したい。ただしプレイ中にEscキーでプレイがキャンセルされた場合、queueが削除できるかどうか確認し、削除できるのであれば、そのように対処したい。

2. [x] カメラ初期化処理をカメラ撮影時だけではなく、アプリ起動時に移動して常にカメラを使える状態とし、カメラ撮影の画面での遅延を回避する

3. [x] カメラ撮影時の画像表示域に顔を中央に納めてもらうための少し縦長楕円のガイドを表示したい。明るい水色の点線を表示するイメージです。

4. [x] /results直下にrecentとranking_topのプレイ結果フォルダ(./results/yyyymmdd_hhmmss)へのリンクリストをresults.jsonファイルで出力したい。結果表示時にrecentには単純に追記して上限個数を超えていたら一番古いものを1つ削除、ranking_topにはスコアを比較し該当箇所に入れ込んで、こちらも上限個数を超えていたらランキング再開のものを1つ削除する仕組み。

5. [x] 可能であれば、二重起動防止を実現する（electronアプリ同士）。`main.ts` の `requestSingleInstanceLock` で実装済み（2つ目は起動せず、既存ウィンドウを前に出す）。なお 2026-09-04、これが効いているのに**残ったプロセスがロックを握って次の起動を妨げる**事故が起きたため、終了経路とバッチ側の後始末も直してある（コミット 5ae920d / e0ed383）。

6. [ ] ComfyUI側の物理絶対パス（ルート、input、output）をconfig.jsonに持つ

7. [ ] アプリからComfyUI起動バッチ実行（TestPageにて）、Powershellウィンドウは開いたままとしたい

8. [ ] resultsフォルダの個人写真のみ削除バッチ

9. [ ] 結果フォルダ内に画像生成バッチを配置し、ComfyUIへ画像生成を再度依頼できるようにしたい。

10. [ ] 設定画面の保存時に、生成パラメータの警告を画面へ表示する（2026-09-03 レビュー2巡目 #2 の残り）。`main.ts` の `save-config` / `reload-config` は `checkGenerationWiring` / `checkGenerationApplied` の結果を `warnings` として返すところまで実装済みだが、`preload.ts` の戻り値型・`ConfigContext.SaveConfigResult`・`TestPage` が未対応で画面に出ない。このため UI から `denoise: 1`（許容範囲内）を保存しても「保存しました」だけが出て、**写真が輪郭しか使われない状態に気づけない**（コンソールと次回起動時ダイアログには出る）。残作業のうち運用上いちばん影響が大きい。

11. [ ] 設定画面のスカラー入力欄を空にしたとき、その項目だけ黙って旧値のまま「保存しました」と出るのを直す（2026-09-03 レビュー2巡目 #7）。対象は制限時間・レベルアップ間隔・部分点率・最大ステージ数・カメラ幅/高さ・生成パラメータ9項目。`TestPage.tsx` の `parseOptionalNumber` が空文字をエラーにせず `undefined` を返し、パッチから落ちるため。ランク閾値とステージ進行には同じ穴を塞ぐガードを入れてあるので、それに合わせる（config 側に既存値がある項目だけ「空欄です」とエラーにする）。設定が壊れるわけではなく、旧値が残る。

12. [ ] 共通レイヤ `comfyui.generation` の扱いを直す（2026-09-03 レビュー2巡目 #8）。(a) 設定画面は共通側＋プロファイル側を重ねて表示し、保存時に**その全部をプロファイル側へ書く**ため、初回保存で共通既定値がプロファイルへ複製され、以後 `config.json` の共通側を編集しても効かなくなる（変更した項目だけ送るようにする）。(b) 旧フラット形式（`profiles` 無し）では `ConfigPatch` に書き込み先が無く、生成パラメータの編集が黙って捨てられる。**現行 `config.json` は共通側 generation が空で profiles 形式のため、いまは実害なし。**

13. [ ] `denoise: 0.6`（2026-09-03 に img2img 化）で実際に生成した絵を確認し、`denoise` と `controlnetStrength` を詰める。**未実施**。ローカルは CPU 実行で 1 枚に時間がかかるため、`tools/comfyui-smoke.cjs`（アプリと同じ生成パラメータを投げるよう修正済み）か、設定画面の「同じワークフローを書き出す」で出した JSON をブラウザの ComfyUI へドラッグ＆ドロップして数枚見る。`steps` は 8 から動かさないこと（denoise を下げてもサンプリング回数は steps のままで、増やすと 1 枚あたりの時間が直接伸びる）。

14. [ ] 設定画面（テスト・設定ページ）の実機確認。**未実施**。config.json の展開・保存ボタン・「同じワークフローを書き出す」→ ブラウザ ComfyUI へ D&D → LoadImage へ写真を D&D → Queue の一連を Electron アプリ上で通す。単体テストと config.json のコピーを使った保存シミュレーションまでは実施済み。

---

2026-09-08: 昨年（よけまくり中）の項目3件を削除した。
「落ちてくる障害物のスプライト」「プレミアムアイテム」「左右キーの大きさ」で、
いずれも昨年の避けゲーのもの。今年のグミパズルには該当する UI も概念も無い
（src/renderer に左右キーの実装は存在しない）。番号は詰め直してある。
