1. [x] 画像生成のトリガーがGAMEOVERタイミングだと遅いので、カメラ撮影が確定した瞬間にphoto_yyyymmdd_hhmmss.pngが生成された時に実行したい。ただしプレイ中にEscキーでプレイがキャンセルされた場合、queueが削除できるかどうか確認し、削除できるのであれば、そのように対処したい。

2. [x] カメラ初期化処理をカメラ撮影時だけではなく、アプリ起動時に移動して常にカメラを使える状態とし、カメラ撮影の画面での遅延を回避する

3. [x] カメラ撮影時の画像表示域に顔を中央に納めてもらうための少し縦長楕円のガイドを表示したい。明るい水色の点線を表示するイメージです。

4. [x] /results直下にrecentとranking_topのプレイ結果フォルダ(./results/yyyymmdd_hhmmss)へのリンクリストをresults.jsonファイルで出力したい。結果表示時にrecentには単純に追記して上限個数を超えていたら一番古いものを1つ削除、ranking_topにはスコアを比較し該当箇所に入れ込んで、こちらも上限個数を超えていたらランキング再開のものを1つ削除する仕組み。

5. [x] 可能であれば、二重起動防止を実現する（electronアプリ同士）。`main.ts` の `requestSingleInstanceLock` で実装済み（2つ目は起動せず、既存ウィンドウを前に出す）。なお 2026-09-04、これが効いているのに**残ったプロセスがロックを握って次の起動を妨げる**事故が起きたため、終了経路とバッチ側の後始末も直してある（コミット 5ae920d / e0ed383）。

6. [x] ComfyUI側の物理絶対パス（ルート、input、output）をconfig.jsonに持つ。
   `comfyui.profiles.<名前>.paths` に `root` / `input` / `output` / `startBat` を持たせた
   （共通側 `comfyui.paths` にも書ける）。`local` / `local_light` に
   `C:\WORK\AI\ComfyUI_20260902_0.34.0\ComfyUI` を入れ、`server` は別の機体なので**持たせない**。
   解決は `comfyui-config.ts` の `resolveComfyUIPaths`。`root` は絶対パス必須で、
   相対だと Electron の作業フォルダ基準で見当違いの場所を掘るため**使わずに警告へ回す**。
   `input` / `output` / `startBat` は省略か `root` 相対でよく、版を差し替えるときは `root` の1行だけ直す。
   プロファイル側に `paths` があれば共通側と**混ぜず丸ごと**そちらを使う（`root` だけ差し替えて
   `input` を共通側から拾うと別の版フォルダの input を見にいくため）。単体テスト `tools/test-comfyui-paths.cjs`。

7. [x] アプリからComfyUI起動バッチ実行（TestPageにて）、Powershellウィンドウは開いたままとしたい。
   テスト・設定ページに［ComfyUI を起動する］［input を開く］［output を開く］を追加した。
   起動は `comfyui-launcher.ts`。`powershell.exe -NoExit -Command "& '<startBat>'"` を直に spawn する。
   ⚠️ **実測でわかった2つの罠**（2026-09-08）:
   - `cmd.exe /c start ...` を挟む形は **Node の引数クォートでは通らない**
     （終了コード 1・バッチは走らず・メッセージも出ない）
   - `detached: true` は Windows で DETACHED_PROCESS になり、**子はコンソールを一切持たない**。
     窓が出ないどころかバッチ自体が走らなかった（終了コード 0 なのに何も起きない）
   そのため `detached: false` で powershell を直に起こす。窓は Windows が割り当てる
   （コンソールを持たない Electron からコンソールアプリを起こすと新しい窓が付く）。
   `-NoExit` は**必須**——モデル読み込みの進みと失敗の理由はその窓にしか出ず、
   当日スタッフはログファイルを開けない。ComfyUI を止めるのもその窓を閉じる操作。
   叩くパスはレンダラから受け取らず、main が config.json で解決したものだけを使う。
   すでにヘルスチェックが通る場合は起動せず「すでに動いています」と返す。
   🔵 **Electron 上で窓が本当に出るかは #14 の実機確認で見ること。**
   ここまでは node から `launchComfyUI` を呼んで、バッチが走り PowerShell が居残ることを確認済み。

8. [~] resultsフォルダの個人写真のみ削除バッチ。
   実処理は `tools/purge-photos.cjs` として**実装済み**（既定ドライラン・`--apply` で実行・
   カードが完成した回だけを対象・`result.json` の参照も外す。単体テスト `tools/test-purge-photos.cjs`）。
   残っているのは**ダブルクリックで動くバッチにするか**の判断だけ。いまは
   `node tools/purge-photos.cjs --apply` を打つ必要がある。
   作るなら `再生成.bat` と同じ薄いラッパの形（`tools/place-regen-bat.cjs` が手本）。

9. [~] 結果フォルダ内に画像生成バッチを配置し、ComfyUIへ画像生成を再度依頼できるようにしたい。
   `tools/place-regen-bat.cjs` で各結果フォルダへ `再生成.bat` を置ける（中身は
   `retry-failed.cjs --apply --only <日時>` を呼ぶだけの薄いラッパ。単体テスト `tools/test-regen-bat.cjs`）。
   残っているのは**まとめて置く運用のままでよいか**の判断。いまは1回のプレイごとに自動では置かれず、
   `node tools/place-regen-bat.cjs --apply` を後から流す形。
   プレイのたびに自動で置くなら `results-manager.ts` の結果フォルダ作成に足す。

10. [x] 設定画面の保存時に、生成パラメータの警告を画面へ表示する（2026-09-03 レビュー2巡目 #2 の残り）。
    `preload.ts` の戻り値型・`ConfigContext.SaveConfigResult.warnings`・`TestPage` の
    「【要確認】」表示まで**通っている**（保存・再読み込み・書き出しの3経路すべて）。

11. [x] 設定画面のスカラー入力欄を空にしたとき、その項目だけ黙って旧値のまま
    「保存しました」と出るのを直す（2026-09-03 レビュー2巡目 #7）。
    `TestPage.tsx` の `parseOptionalNumber` が第4引数に現在値を取り、
    **config に値がある項目が空欄ならエラーにする**形になっている。
    制限時間・レベルアップ間隔・部分点率・最大ステージ数・カメラ幅/高さ・生成パラメータ9項目に適用済み。

12. [ ] 共通レイヤ `comfyui.generation` の扱いを直す（2026-09-03 レビュー2巡目 #8）。(a) 設定画面は共通側＋プロファイル側を重ねて表示し、保存時に**その全部をプロファイル側へ書く**ため、初回保存で共通既定値がプロファイルへ複製され、以後 `config.json` の共通側を編集しても効かなくなる（変更した項目だけ送るようにする）。(b) 旧フラット形式（`profiles` 無し）では `ConfigPatch` に書き込み先が無く、生成パラメータの編集が黙って捨てられる。**現行 `config.json` は共通側 generation が空で profiles 形式のため、いまは実害なし。**

13. [ ] 実写の顔で生成した絵を確認し、`denoise` と `controlnetStrength` を詰める。**未実施**。なお `denoise` は 2026-09-07 に 0.6 → **0.9** へ上げてある（0.6 では教室の背景が残り、アニメ絵になっていなかった）。残っているのは `controlnetStrength`（現在 0.85）の詰めと、0.9 が実写の顔で妥当かの確認。🔴 **判断は必ず実写の顔で行うこと**——`dummy_photo.png` はもとからアニメ絵なので差がほとんど見えない。ローカルは CPU 実行で 1 枚に時間がかかるため、`tools/comfyui-smoke.cjs`（アプリと同じ生成パラメータを投げるよう修正済み）か、設定画面の「同じワークフローを書き出す」で出した JSON をブラウザの ComfyUI へドラッグ＆ドロップして数枚見る。`steps` は 8 から動かさないこと（denoise を下げてもサンプリング回数は steps のままで、増やすと 1 枚あたりの時間が直接伸びる）。

14. [ ] 設定画面（テスト・設定ページ）の実機確認。**未実施**。今回（2026-09-08）足した［ComfyUI を起動する］［input を開く］［output を開く］も含める。起動ボタンで見るのは**PowerShell の窓が実際に開き、開いたまま残ること**——Electron（コンソールを持たない GUI プロセス）から起こしたときの挙動は node からは確かめられない。config.json の展開・保存ボタン・「同じワークフローを書き出す」→ ブラウザ ComfyUI へ D&D → LoadImage へ写真を D&D → Queue の一連を Electron アプリ上で通す。単体テストと config.json のコピーを使った保存シミュレーションまでは実施済み。

15. [~] 当日PCへのセットアップ（USB 1本 → C:\kidspg で完結 → 起動バッチだけ）。
    **作成側は完成している**: `tools/onsite/build-materials.ps1`（リポジトリ外の資材を組む）→
    `tools/make-onsite-package.cjs`（USB へ書き出す）→ 当日PC で `0_セットアップ.bat`。
    手順は docs/setup-onsite.md、判断の理由は docs/distribution-plan.md。
    起動バッチは **ゲーム画面が本当に出たこと**を `logs/ready.json` で確かめてから
    「★★★ 準備完了 ★★★」を出す（`src/main/services/readiness.ts` の blockers / notes）。
    開発機では実測で通っている（ComfyUI 待受 → 画面表示 → 準備完了、`%APPDATA%` は無変更）。

    🔴 **残っているのは2つだけ。どちらも別PCが要る。**
    - **USB を作り直す。** `D:\KidsPG2026_setup` は 2026-09-09 の敵対的レビュー
      （3巡）より前に作ったもので、ops への assets 同梱・start-comfyui.bat の
      `ping` 待ち・準備判定の blockers/notes・VC++ の同梱などが**入っていない**。
    - **別PCでの検証。** Smart App Control の判定が
      「オフラインにして再起動しても残るか」は当該機体でしか確かめられない。
      前日までに `ウォームアップ.bat` をネット有りで通し、ブロック 0 件を確認 →
      ネットを切って再起動 → `start-kidspg.bat` で準備完了が出ることまで見る。

---

2026-09-08: 昨年（よけまくり中）の項目3件を削除した。
「落ちてくる障害物のスプライト」「プレミアムアイテム」「左右キーの大きさ」で、
いずれも昨年の避けゲーのもの。今年のグミパズルには該当する UI も概念も無い
（src/renderer に左右キーの実装は存在しない）。番号は詰め直してある。

2026-09-08: 項目 6・7 を実装した（ComfyUI の物理パスと、アプリからの起動）。
あわせて 8〜11 の状態を実際のコードと突き合わせて直した。8・9 は実処理が
tools/ に実装済みで、残っているのは「ダブルクリックで動く形にするか」「自動で置くか」の
運用判断だけだったため [~] にしてある。10・11 は完了していたのに [ ] のままだった。

2026-09-09: 当日PC向けの配布・セットアップ一式（項目 15）を作り、敵対的レビューを
4巡かけた。直したもののうち当日に効くものは「遊べるのに開場を止める／正常なのに
正常と言わない」判定の逆転、配布された `ops\` の形で救済ツールが1つも動かなかったこと、
ComfyUI の複製で `comfy_api\input` が消えていたこと、そして文書と実装の食い違い。
テストは 169 → 249 件に増えている。
